git init # git Initialized

git config --global core.autocrlf true

git remote add origin <리포지토리 url>

git add . # 현재 디렉토리에서 올릴 파일 로컬에서 커밋 대상에 추가

git commit -m "<메시지>" # git 커밋으로 add한 파일 프로비저닝

git push origin master # github에 푸쉬
# git push origin +master